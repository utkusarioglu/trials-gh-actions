name: Push
run-name: run-push
  
on:
  push:

jobs:
  db-trial:
    runs-on: ubuntu-latest
    container:
      image: node:latest
    services:
      nginx-service:
        image: nginx:latest
        ports:
          - 80:80
    steps:
      - run: echo hello
      - run: |
          node scripts/request.js
      - run: echo done
