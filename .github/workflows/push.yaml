name: Push

# cats
on: 
  push:
    
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: 
          - ubuntu-latest
          - ubuntu-20.04
        include:
          - os: ubuntu-latest
            cat: dog
    steps:
      - run: |
          echo "${{ matrix.os }} -${{ matrix.cat }}-" 
     